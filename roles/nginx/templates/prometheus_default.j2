server {
    listen 80 default_server;
    server_name _;

    location /prometheus {
        proxy_pass http://Kryptograafia-2:9090/prometheus;  # Prometheus default port
    }
    
    location / {
        deny all;
    }
}

server {
    listen 8080 default_server;

    location = /stub_status {
        stub_status;
    }
}
