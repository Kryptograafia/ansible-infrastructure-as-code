server {
    listen 80 default_server;
    server_name _;

    location / {
        proxy_set_header Host $http_host;
        proxy_pass http://Kryptograafia-1:{{ agama_port }};
    }

    location = /basic_status {
        stub_status;
    }

    location /grafana {
        proxy_set_header Host $http_host;
        proxy_pass http://Kryptograafia-1:{{ grafana_port }};
    }
}

server {
    listen 8080 default_server;

    location = /stub_status {
        stub_status;
    }
}
